[phases.setup]
nixPkgs = ["python3", "gcc", "pkg-config", "libmysqlclient", "default-libmysqlclient-dev"]

[phases.build]
dependsOn = ["install"]
cmds = [
    "python manage.py migrate --noinput"
]

[start]
cmd = "gunicorn config.wsgi:application --bind 0.0.0.0:8000"